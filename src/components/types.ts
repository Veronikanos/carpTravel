export type Slide = {
  id: number;
  picture: string;
  title: string;
  bg: string;
  description: string;
};

export type ServicesSlideProps = {
  className?: string;
  picture: any;
  bg: any;
  title: string;
};