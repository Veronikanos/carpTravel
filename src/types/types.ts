export type Slide = {
  id: number;
  picture: string;
  title: string;
  bg: string;
  description: string;
};

export type Gallery = {
  id: number;
  picture: string;
  title: string;
};

export type ServicesSlideProps = {
  picture: any;
  title: string;
};
